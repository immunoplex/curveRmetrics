% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inflection_point_sensitivity.R
\name{.inflection_x}
\alias{.inflection_x}
\title{Compute analytical inflection x-coordinate from normalised parameters}
\usage{
.inflection_x(p)
}
\arguments{
\item{p}{Named list with family, a, b, c, d, g (all natural scale)}
}
\value{
Numeric scalar, inflection x on natural concentration scale
}
\description{
All inputs are assumed to be on the natural concentration scale.
Derivations:
}
\details{
logistic4:
y = d + (a-d) / (1 + exp((x-c)/b))
Symmetric => inflection exactly at x = c, y = (a+d)/2

logistic5:
y = d + (a-d) / (1 + exp((x-c)/b))^g
Set d²y/dx² = 0: let z = exp((x-c)/b)
Numerator of d²y/dx² proportional to: (g+1)\emph{z - (b-1)/(b}... )
Full derivation => z* = (b-1) / (b\emph{g + 1)  requires b > 1 and z} > 0
=> x_infl = c + b * log(z*)

loglogistic4:
y = a + (d-a) / (1 + (x/c)^b)
Symmetric on log(x) scale => inflection at x = c

loglogistic5:
y = a + (d-a) * (1 + g\emph{exp(-b}(x-c)))^(-1/g)
Set d²y/dx² = 0: let u = g\emph{exp(-b}(x-c))
=> u* = (b-1)/(b+1)  ... simplified Richards condition  requires b > 1
=> x_infl = c - (log(u*) - log(g)) / b
= c + (log(g) - log((b-1)/(b+1))) / b

gompertz4:
y = a + (d-a) * exp(-exp(-b*(x-c)))
Set d²y/dx² = 0: let u = exp(-b*(x-c))
=> u*(1 - u) = 1/b ... simplifies to u* = (b-1)/b  (requires b > 1)
=> x_infl = c - log(u*) / b = c + log(b/(b-1)) / b
At inflection: y_infl = a + (d-a)*exp(-1)  (always ~36.8\% of range from a)
}
\keyword{internal}
