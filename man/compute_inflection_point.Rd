% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inflection_point_sensitivity.R,
%   R/parameter_normalization_functions.R
\name{compute_inflection_point}
\alias{compute_inflection_point}
\title{Add inflection-point coordinates to a curves data frame}
\usage{
compute_inflection_point(curves_df, verbose = TRUE)

compute_inflection_point(curves_df, verbose = TRUE)
}
\arguments{
\item{curves_df}{A data frame with one row per curve containing at minimum
\code{curve_id}, \code{method}, \code{model_name}, \code{a}, \code{b},
\code{c}, \code{d}, and \code{g}.  Frequentist rows hold raw
\eqn{\log_{10}} parameters; Bayesian rows must also carry \code{*_nat}
columns.}

\item{verbose}{Logical; if \code{TRUE} (default) emits a message per curve
with the computed inflection coordinates.}
}
\value{
\code{curves_df} with two additional numeric columns: \code{inflect_x} and
\code{inflect_y}.

\code{curves_df} with two new (or replaced) columns:
\code{inflect_x} and \code{inflect_y}, both on the natural scale.
}
\description{
Applies compute_inflection_point_worker() to every row of \code{curves_df}
and appends two new columns: \code{inflect_x} (concentration at the inflection
point) and \code{inflect_y} (predicted response at the inflection point).
Both values are on the same scale as \code{c} after normalisation by
.normalise_params().

Iterates over unique \code{(curve_id, method)} combinations, resolves
parameters via \code{\link{.inflect_x_analytical}}, evaluates the response at
that concentration, and joins the results back onto the input data frame.
}
\details{
The inflection point is defined as the concentration at the midpoint of the
response range (\eqn{y^* = \sqrt{a \cdot d}}), computed analytically without
root-finding.
}
