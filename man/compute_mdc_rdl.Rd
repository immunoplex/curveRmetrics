% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lod_functions.R
\name{compute_mdc_rdl}
\alias{compute_mdc_rdl}
\title{Compute minimum detectable concentrations and reliable detection limits}
\usage{
compute_mdc_rdl(param_ci_df, lods, verbose = TRUE)
}
\arguments{
\item{param_ci_df}{Long-format tibble of parameter estimates and CIs,
as passed to \code{\link[=generate_lods]{generate_lods()}}.}

\item{lods}{Tibble returned by \code{\link[=generate_lods]{generate_lods()}}, with columns
\code{curve_id}, \code{llod}, and \code{ulod}.}

\item{verbose}{Logical. If \code{TRUE}, emits one diagnostic message per curve.}
}
\value{
Tibble with columns:
\describe{
\item{\code{curve_id}}{Curve identifier.}
\item{\code{mindc}}{Lower MDC. Log10-concentration for frequentist;
natural concentration for Bayesian.}
\item{\code{maxdc}}{Upper MDC. Same scale convention as \code{mindc}.}
\item{\code{minrdl}}{Lower RDL (conservative bound). Same scale as \code{mindc}.}
\item{\code{maxrdl}}{Upper RDL (liberal bound). Same scale as \code{mindc}.}
}
}
\description{
Back-calculates concentration values from LOD response thresholds using
curve inversion:
\describe{
\item{MDC (minimum detectable concentration)}{Inversion of the
point-estimate curve at LLOD (\code{mindc}) and ULOD (\code{maxdc}).}
\item{RDL (reliable detection limit)}{Inversion of CI-bound curves:
\code{minrdl} uses a compressed curve (\code{a = ci_upper["a"]},
\code{d = ci_lower["d"]}); \code{maxrdl} uses an expanded curve
(\code{a = ci_lower["a"]}, \code{d = ci_upper["d"]}).}
}
}
\details{
For Bayesian curves the inversion result is in log10-concentration (matching
the normalised parameter scale); it is converted back to natural
concentration via \eqn{10^x} before storage so that downstream plotting
code can apply \code{log10()} uniformly.
}
