% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inflection_point_sensitivity.R,
%   R/parameter_normalization_functions.R
\name{compute_assay_sensitivity}
\alias{compute_assay_sensitivity}
\title{Estimate assay sensitivity as the slope at the inflection point}
\usage{
compute_assay_sensitivity(curves_df, verbose = TRUE)

compute_assay_sensitivity(curves_df, verbose = TRUE)
}
\arguments{
\item{curves_df}{A data frame with one row per curve.  Must contain
\code{curve_id}, \code{method}, \code{model_name}, and \code{inflect_x}
(natural-scale, as produced by \code{\link{compute_inflection_point}}),
plus raw or \code{*_nat} parameter columns.}

\item{verbose}{Logical; if \code{TRUE} (default) emits a message per curve
with \code{inflect_x} and the estimated slope.}
}
\value{
Tibble with columns:
\describe{
\item{\code{curve_id}}{Curve identifier.}
\item{\code{method}}{\code{"frequentist"} or \code{"bayesian"}.}
\item{\code{inflect_x}}{Inflection-point concentration (log10 scale).}
\item{\code{dydx_inflect}}{First derivative of response with respect to
log10-concentration at the inflection point.}
}

A data frame with columns \code{curve_id}, \code{method},
\code{inflect_x}, and \code{dydx_inflect}.
}
\description{
Computes the first derivative of the forward curve at the inflection point
using a central-difference approximation.  A larger absolute value indicates
greater assay sensitivity (larger response change per unit change in
log10-concentration).

Estimates \eqn{dy/dx} at \code{inflect_x} for each curve using a symmetric
central finite difference with step size \eqn{h = |x| \times 10^{-5}}.
Parameters are normalised via \code{\link{.normalise_params}} before
evaluation.
}
\details{
The inflection x-coordinate is taken directly from \code{curves_df$inflect_x};
run compute_inflection_point() on the data frame first.
}
